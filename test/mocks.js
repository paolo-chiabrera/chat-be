const MOCK_USERNAME = 'test';

const MOCK_USER = {
  color: '#000',
  username: MOCK_USERNAME
};

const MOCK_MESSAGES = [];

const MOCK_CONTENT = 'content';

const MOCK_UNSAFE_CONTENT = '<h1>safe</h1>';

const MOCK_SAFE_CONTENT = 'safe';

module.exports = {
  MOCK_CONTENT,
  MOCK_MESSAGES,
  MOCK_SAFE_CONTENT,
  MOCK_USER,
  MOCK_USERNAME,
  MOCK_UNSAFE_CONTENT
};
